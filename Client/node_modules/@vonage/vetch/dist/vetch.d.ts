import { VetchOptions, VetchPromise } from './common';
export declare class Vetch {
    defaults: VetchOptions;
    constructor(defaults?: VetchOptions);
    private _defaultAdapter;
    request<T = any>(opts?: VetchOptions): VetchPromise<T>;
    private getResponseData;
    private validateOpts;
    private checkStatus;
    private createResponse;
}
